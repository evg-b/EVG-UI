export default function ConvertColor(color = '', format = 'hex') {
    let formatFinal = ''
    switch (format) {
        case 'rgb':

            break;
        case 'rgba':

            break;
        case 'hsl':

            break;
        case 'hsla':

            break;
        case 'hexa':

            break;

        default:
            formatFinal = 'hex'
            break;
    }
}