---
id: Tooltip
title: Tooltip
sidebar_label: Tooltip
---

import {
	TooltipBaseExample,
	TooltipPositionExample,
	TooltipAutoHideExample,
	TooltipAnimationExample,
} from '../../Example/Components/Tooltip/Examples'
import ApiDoc from '../../Example/ApiDoc/ApiDoc'

Всплывающая подсказка когда пользователь наводит курсор на элемент.

## Base Tooltip

<TooltipBaseExample />

## Position

Вот все позиции которые может принимать `Tooltip` относительно `target`.

`top`, `top-left`, `top-right`,

`bottom`, `bottom-left`, `bottom-right`,

`left`, `left-top`, `left-bottom`,

`right`, `right-top`, `right-bottom`.

<TooltipPositionExample />

## Animation

Виды анимации появления `fade`, `docking`, `zoom`.

<TooltipAnimationExample />

## API

<ApiDoc name='Tooltip' />