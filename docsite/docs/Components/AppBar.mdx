---
id: AppBar
title: AppBar
sidebar_label: AppBar
---

import {
    AppBarBaseExample,
    AppBarColorExample,
    AppBarLeftAndRightExample,
    AppBarTitleCenterExample,
} from '../../Example/Components/AppBar/Examples'
import ApiDoc from '../../Example/ApiDoc/ApiDoc'

Это верхняя панель управления обычно используется для заголовков, навигации и действий связанных с текущим экраном.

## Base AppBar

<AppBarBaseExample />

## Color

Цвет компонента меняется через свойство `color`. Более подробно про цвет в этой [статье](/docs/styles/color#props-color).

<AppBarColorExample />

## left/right components

Свойства `left` и `right` это контейнеры для размещения `node` элементов, таких как `Button`, `Icons` и д.р.

<AppBarLeftAndRightExample />

## titelCenter

Если свойство `titleCenter={true}`, titel будет отображаться в центре. 

<AppBarTitleCenterExample />

## API

<ApiDoc name='AppBar' />