---
id: Avatar
title: Avatar
sidebar_label: Avatar
---

import {
    AvatarBaseExample,
	AvatarColorExample,
	AvatarNotImageExample,
	AvatarSizeExample,
	AvatarStatusExample,
	AvatarStatusColorExample,
	AvatarGroupAndSizeExample,
	AvatarMultipleExample,
} from '../../Example/Components/Avatar/Examples'
import ApiDoc from '../../Example/ApiDoc/ApiDoc'

Этот компонент обычно используют для отображения изображения профиля пользователей. Так же отображает первую букву в имени контакта если изображение еще не загрузилось.

## Base Avatar

<AvatarBaseExample />

## Avatar not image

Если нету картинки, отображается первая буква `alt`. Если `alt` не определен то отображается иконка по умолчанию. 

Свойство `altMax={number}` определяет количество букв для показа.

<AvatarNotImageExample />

## Color

Цвет компонента меняется через свойство `color`. Более подробно про цвет в этой [статье](/docs/styles/color#props-color).

<AvatarColorExample />

## Size

Размер компонента изменяется через свойство `size`.

<AvatarSizeExample />

## Status

Можно отображать онлайн статус если `status={true}`

<AvatarStatusExample />

## StatusColor

<AvatarStatusColorExample />

## Multiple avatar

Свойство `srcs={[string]}` принимает массив ссылок на изображения.

<AvatarMultipleExample />

## AvatarGroup

<AvatarGroupAndSizeExample />

## API

<ApiDoc name='Avatar' />