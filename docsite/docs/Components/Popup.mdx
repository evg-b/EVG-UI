---
id: Popup
title: Popup
sidebar_label: Popup
---
import {
    PopupBaseExample,
    PopupPositionExample,
    PopupShiftExample,
	PopupAnimationExample,
    PopupAutoHideExample,
    PopupModeExample,
    PopupScrollableExample,
} from '../../Example/Components/Popup/Examples'
import ApiDoc from '../../Example/ApiDoc/ApiDoc'

Popup - компонент для создания всплывающего окна.

## Base

<PopupBaseExample />

## Position

Вот все позиции которые может принимать `Popup` относительно `target`.

`top`, `top-left`, `top-right`,

`bottom`, `bottom-left`, `bottom-right`,

`left`, `left-top`, `left-bottom`,

`right`, `right-top`, `right-bottom`.

<PopupPositionExample />

## Shift

Сдвиг от `target`, положительное или отрицательное число.

<PopupShiftExample />

## animation

Виды анимации появления `fade`, `docking`, `zoom`.

<PopupAnimationExample />

## Hover или Click

`Popup` может открываться как при наведении так и при нажатии указателя.

<PopupModeExample />

## Scrollable

`Popup` устойчив к перемещению своей цели, несмотря на то что они находятся в разных DOM-узлах.

<PopupScrollableExample />

## API

<ApiDoc name='Popup' />